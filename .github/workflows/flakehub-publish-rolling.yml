name: "Publish to FlakeHub"
on:
  push:
    branches:
      - "main"
jobs:
  flakehub:
    runs-on: "ubuntu-latest"
    permissions:
      id-token: "write"
      contents: "read"
    steps:
      - uses: "actions/checkout@v3"
      - uses: "DeterminateSystems/nix-installer-action@main"
      - uses: "DeterminateSystems/flakehub-push@main"
        with:
          name: "alythical/kyanite"
          rolling: true
          visibility: "unlisted"
